- [ ] 1. Add OpenSpec delta specs for Orchestrator task contracts and `write_artifact.outputKey`.
- [ ] 2. Update numbered Orchestrator and tools specs with the same contract.
- [ ] 3. Extend shared dispatch plan types and `plan_tasks` schema.
- [ ] 4. Implement plan compilation and validation for `inputs` and `expectedOutputs`.
- [ ] 5. Add `outputKey` support to `write_artifact` and collect output-key artifact mappings.
- [ ] 6. Inject handoff contract blocks into child prompts and enforce required inputs/outputs.
- [ ] 7. Update the dispatch plan card to display inputs, outputs, and acceptance criteria.
- [ ] 8. Add focused regression tests and run validation.
